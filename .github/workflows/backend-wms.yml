name: Backend WMS CI/CD

on:
  push:
    branches: [ "main" ]
    paths:
      - 'team1-backend-wms'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'team1-backend-wms'

permissions:
  contents: write

concurrency:
  group: k8s-manifests-update
  cancel-in-progress: false

jobs:
  call-workflow:
    uses: ./.github/workflows/java-ci-cd.yml
    with:
      service_name: wms-service
      working_directory: team1-backend-wms
      image_name: wms-service
      use_dockerhub: true
    secrets: inherit
